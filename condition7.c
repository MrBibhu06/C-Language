#include<stdio.h>
int main(){
	int a,num;
	printf("enter a number =");
	scanf("%d",&num);
	if(num%2 == 0)
	printf("the number is divisible by the number");
	else
	printf("the number is not divisible");
	return 0;
}
